import { MenuItemModel } from '@shared-primeng/layout'

export const mockNavBarOptions = (): MenuItemModel[] => {
    return [
        /* {
            route: ['auth', 'login'],
            icon: 'person'
        },
        {
            route: ['auth', 'sign-up'],
            icon: 'group_add'
        },
        {
            route: ['home', 'news'],
            icon: 'autorenew'
        } */]
}